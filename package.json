{
  "name": "tray-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "stage": "node scripts/stage-assets.mjs",
    "dev": "npm run stage && wrangler dev",
    "deploy": "npm run stage && wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

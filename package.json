{
  "name": "tray-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "stage": "node scripts/stage-assets.mjs",
    "build:assets": "node scripts/checkout-catalog-repos.mjs && npm run stage",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "verify:deployment": "node scripts/verify-deployment.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "decode-ico": "^0.4.1",
    "gifsicle-wasm": "^1.96.4",
    "sharp": "^0.35.3",
    "wrangler": "^4.0.0"
  }
}
